node {
	agent any

	stages {
		stage('Build') {
			steps {
				echo 'Build'
			}
		}

		stage('Test') {
			steps {
				echo 'Tets'
			}
		}
		stage('Deploy') {
			steps {
				echo 'Deploy'
			}
		}
	}
}
